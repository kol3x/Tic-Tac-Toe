// Gameboard module 

const gameBoard = (() => {
    let state = ['X', 'O', 'x',
                 'X', 'X', 'O',
                 'O', 'O', 'X'];
    return {state};
})();

// Player factory func

// const Player = (todo) => {
    
// };

document.addEventListener('DOMContentLoaded', function() {
    
});

